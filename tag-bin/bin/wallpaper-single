#!/bin/bash

shopt -s nullglob

cd /data/images/wallpaper/

files=()
for i in *.jpg *.png; do
	[[ -f $i ]] && files+=("$i")
done
range=${#files[@]}

((range)) && feh --bg-fill "${files[RANDOM % range]}"
