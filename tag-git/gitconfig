[user]
  email = michel.rouly@gmail.com
  name = Jean Michel Rouly
  signingkey = 1DFE3B2C
[push]
  default = simple
[pull]
  rebase = true
[commit]
  gpgSign = true
[core]
  editor = vim
[alias]
  lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
  s = status
  a = commit -a --amend
  prm = pull --rebase upstream master
  clone = clone -o upstream
  co = checkout
[http]
  sslVerify = false
[credential]
  helper = cache --timeout 18000
